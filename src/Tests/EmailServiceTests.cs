using System;
using System.Collections.Generic;
using System.IO;
using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.DependencyInjection;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Moq;
using UofUStudentVerificationBot;

namespace UofUStudentVerificationBotTests
{
    // [TestClass]
    public class EmailServiceTests
    {

        // [TestMethod]
        // public void SendEmail_x_x()
        // {
        //     IConfiguration config = new ConfigurationBuilder()
        //         .SetBasePath(Directory.GetCurrentDirectory())
        //         .AddJsonFile(Path.GetFullPath("../../../../config.json"), optional: false, reloadOnChange: true)
        //         .AddEnvironmentVariables()
        //         .Build();
        //     Assert.Fail();
        //     IEmailService emailService = new EmailService(config);
        //     emailService.SendEmail("u1216122", "696969");
        //     Assert.Fail();
        // }

    }
}